<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PotpyTown" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/ivory</item>
        <item name="android:navigationBarColor">@color/ivory</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:navigationBarDividerColor">@color/ivory</item>
        <item name="android:selectableItemBackground">@null</item>
        <item name="android:colorControlHighlight">@android:color/transparent</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PotpyTown" parent="Base.Theme.PotpyTown" />

    <style name="SplashTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_img</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemRippleColor">@null</item>
        <item name="itemShapeAppearanceOverlay">@null</item>
    </style>

    <style name="AppTheme.NoRipple" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:colorControlHighlight">@android:color/transparent</item>
    </style>

    <style name="Widget.BottomNavigationView.NoRipple" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:selectableItemBackground">@null</item>
    </style>

</resources>